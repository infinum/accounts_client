require 'spec_helper'

describe Infinum::Accounts do
  it 'has a version number' do
    expect(Infinum::Accounts::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
